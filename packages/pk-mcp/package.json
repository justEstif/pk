{
   "name": "@justestif/pk-mcp",
   "version": "0.1.0",
   "description": "MCP server for pk — shells out to pk CLI",
   "type": "module",
   "bin": {
      "pk-mcp": "dist/index.js"
   },
   "files": [
      "dist/"
   ],
   "scripts": {
      "build": "bun build src/index.ts --outfile dist/index.js --target node && sed -i '1i#!/usr/bin/env node' dist/index.js && chmod +x dist/index.js",
      "check": "bun run typecheck && bun test",
      "typecheck": "bunx tsc --noEmit",
      "prepublishOnly": "bun run build"
   },
   "dependencies": {
      "@modelcontextprotocol/sdk": "^1.29.0",
      "zod": "^4"
   },
   "devDependencies": {
      "@types/bun": "latest",
      "typescript": "latest"
   }
}
