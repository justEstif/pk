{
   "mcpServers": {
      "pk": {
         "command": "${CLAUDE_PLUGIN_ROOT}/bin/pk",
         "args": [
            "mcp"
         ],
         "env": {
            "PK_KNOWLEDGE_DIR": "${CLAUDE_PROJECT_DIR}/.pk"
         }
      }
   }
}
